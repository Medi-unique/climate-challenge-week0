# Notebooks

- `<country>_eda.ipynb` — profiling, cleaning, and EDA for one country (branch per challenge: `eda-<country>`).
- `compare_countries.ipynb` — five-country synthesis (`compare-countries` branch).

Run Jupyter from the **repository root** so imports resolve (`src` on `PYTHONPATH` via `sys.path` in the first code cell).
